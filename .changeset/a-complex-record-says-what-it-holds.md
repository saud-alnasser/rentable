---
'rentable': minor
---

a complex's record now states how many units it holds, how many are occupied, how many are
vacant, and how many contracts run against those units today — where it used to state only the
unit total
