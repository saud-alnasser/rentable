---
'@rentable/desktop': patch
---

the sign-in screen reads as a login page: a title, a line under it, and the way in — the coloured box that used to greet every visit now appears only when an attempt actually fails
