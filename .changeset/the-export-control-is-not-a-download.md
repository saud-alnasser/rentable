---
'rentable': patch
---

a list's export control is drawn as a table with an arrow leaving it, rather than as a download
arrow. nothing is downloaded here — the file is written to disk and revealed in the file manager
