---
'@rentable/desktop': minor
---

lists, records, settings and the dashboard load the same way: a skeleton shaped like what is coming, shown only when a load takes a moment. The note on a locked contract's units now reads as a notice rather than an error
