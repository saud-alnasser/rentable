---
'@rentable/desktop': minor
---

deleting a tenant, a complex, a unit, a payment or a contract happens at once, and the message that follows offers undo and says it lasts while the app is open. a record something still depends on is refused as before, naming what holds it. deleting a workspace or removing a member still asks first, and terminating or restoring a contract, signing out elsewhere, forgetting the account and disconnecting now ask in a dialog named for what they do rather than in the delete dialog
