---
'@rentable/desktop': patch
---

a field that carries a leading glyph (the password on the sign-in screen, a workspace's name) now has the same height, rounding and fill as every other field, instead of a taller bordered box
