---
'rentable': minor
---

settings is one column in four groups — general, workspace, updates, diagnostics — instead of a strip of tiles above a grid of cards. the tiles are gone rather than restyled: every figure on them was already stated by the section that owns it, and the version now lives in updates and nowhere else.
