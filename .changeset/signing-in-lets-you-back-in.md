---
'@rentable/desktop': patch
---

signing in after a spell offline now opens the workspace again instead of returning to the same screen, because a new session no longer inherits the expired credential of the one before it
