---
'@rentable/desktop': patch
---

the breadcrumb links only to pages that exist and ends on the record you are reading, by its name. a record's sections and the settings sections switch with one row of links, so every section has its own address, a contract's history included. the organization walk and the join screen go back with the same control as a record's page
