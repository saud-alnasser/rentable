---
'@rentable/desktop': patch
---

select several complexes, or several of a complex's units, and delete the whole selection at once. the confirmation asks the workspace what would happen rather than reading the rows, so a unit whose contract has not started yet is shown as refused even though the list calls it vacant. one undo puts the set back whole
