---
'@rentable/desktop': minor
---

run an action from the command palette instead of going to the surface that owns it: type its name and it runs where you are standing. an action that acts on a record — renewing a contract — asks which one inside the palette, and one that cannot run right now says so with the reason rather than doing nothing
