---
'@rentable/desktop': patch
'@rentable/design': patch
---

the sheet that adds a member reads like the sheet that edits one: the username under its own line, the role in its tray, what they may do beyond their role as a list you add to, and a row per workspace where no access means not granted. a segment of a segmented control keeps its side padding however narrow the row, so "administrator" no longer touches its edges
