---
'@rentable/desktop': patch
---

select several payments on a contract's statement and delete the whole selection at once, after a confirmation that says how many would go through. one undo puts the set back whole, and the contract's paid amount is recomputed once rather than once per payment
