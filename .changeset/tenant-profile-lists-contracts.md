---
'rentable': minor
---

a tenant's profile now has tabs, and the contracts tab lists what that tenant rents — searched and sorted the same way the contracts directory is, minus the tenant column, which holds the same name on every row there.
