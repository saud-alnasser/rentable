---
'@rentable/desktop': patch
---

signing in from the sidebar menu now opens the sign-in screen instead of going straight to google, so the screen that says what you are signing in with is always shown first
