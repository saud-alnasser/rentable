---
'@rentable/desktop': patch
---

an empty list says what it will hold and offers to add the first record, and a search or filter that matches nothing says so and offers to clear it, so the two no longer read alike. the contract's unit panes, the dashboard and the settings directories use the same treatment. a record that no longer exists says so and offers the way back, and an address that leads nowhere says the page does not exist instead of that the screen could not be shown
