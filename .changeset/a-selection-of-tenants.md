---
'@rentable/desktop': patch
---

select several tenants in the directory and delete the whole selection at once, after a confirmation that says how many would go through and names the ones still holding contracts. one undo puts the set back whole
