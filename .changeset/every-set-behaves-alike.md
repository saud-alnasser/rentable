---
'@rentable/desktop': patch
---

every list can be sorted, a complex's units and a contract's payments included. the contract's unit panes search on the same bar as every other list and show each unit as the same card, with its menu. when a set takes no new records, the "new record" in its empty state is dimmed and says why, as the toolbar's does. a record that no longer exists and an address that leads nowhere look the same and offer one way back. the members section orders its toolbar like every other list, and its descriptions are muted
