---
'@rentable/desktop': patch
---

every list that exports now offers exporting just the records you have selected, in the same columns and under the same file naming as exporting the whole list. the file says how many were selected, so it does not quietly replace the one holding everything
