---
'@rentable/desktop': patch
---

a contract's schedule can be printed or saved as a PDF: it opens in a preview inside the application first, as a one-page document in Arabic or English as chosen, headed by the organization; on Windows a PDF is saved without any print dialog, and printing uses the system's own dialog
