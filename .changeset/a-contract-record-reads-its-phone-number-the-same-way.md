---
'@rentable/desktop': patch
---

a contract record now draws the tenant's phone number the way the tenant record and the directory already do, so in Arabic the country code sits at the start of the number instead of the end
