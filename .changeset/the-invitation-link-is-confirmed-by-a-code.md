---
'@rentable/desktop': minor
---

inviting somebody, and resetting their password, now produce a link to send and a six-character confirmation code to read out on a call. the code is one half of what opens the invited vault and the link's secret is the other, so a link that leaks or is forwarded opens nothing without a code that was alive when it was typed; a code lapses ninety seconds after it is made, the issuer makes a fresh one from the result panel or the pending member's row, and the person opening the link types it above the password they choose. the connect screen no longer names the invited person before the code is typed
