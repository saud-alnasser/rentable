---
'@rentable/desktop': patch
---

a contract's units tab now lists every unit the contract holds, including one that another contract running over the same dates also holds. before, such a unit was left off the tab, so deleting the contract could be refused for units the tab did not show; the count a refusal gives is now the count the tab lists
