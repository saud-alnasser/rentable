---
status: resolved
---

# feat(layout): the workspace menu is the workspace and the switch

## Outcome

The rail's workspace menu is its header, the workspaces the member holds with the open one
marked, and one row to the workspaces section of settings. Invite and new workspace, with
their refusal sentences, are gone from it; both remain in the settings area, where the
members and workspaces sections offer them.

## Acceptance Criteria

Traces requirement 9 of [[efforts/828-the-link-needs-a-code-and-the-settings-area-guides/spec]],
and its criterion 9.

- [x] `layout/component/workspace-menu.svelte` loses the `canInvite`, `canCreateWorkspace`
      and `refusal` props, the side-by-side pair and the create row at the foot; it draws the
      header, the radio group of workspaces with the open one checked, and one full-width row
      to `withSection('workspaces')` at the foot, carrying `data-workspace-menu-workspaces`.
      *Verified 2026-09-15 on the effort branch: the component's props are `workspace`,
      `workspaces`, `openId`, `memberCount`, `onSwitch`; `grep` for `canInvite`,
      `canCreateWorkspace`, `refusal` and `openOrganizationDialog` in it prints nothing; the
      foot is one `DropdownMenu.Item` whose child is the anchor to `withSection('workspaces')`
      carrying the attribute.*
- [x] `layout/component/sidebar.svelte` stops computing `canInvite`, `canCreateWorkspace`
      and `refusal` for the menu; `workspaceMenu.invite`, `workspaceMenu.inviteRefused` and
      `workspaceMenu.workspaceRefusedOwner` are removed from both locales and from
      `i18n-types.ts`; `workspaceMenu.create` and `workspaceMenu.workspaceRefusedAuthority`
      stay, since the workspaces section reads them. *Verified: `grep -rn` for the three keys
      across `apps/desktop/src` and `packages` prints nothing; `git show --stat` has
      `i18n/ar/index.ts | 3 -`, `i18n/en/index.ts | 3 -`, `i18n-types.ts | 24 -` (regenerated by
      `typesafe-i18n --no-watch`); `workspaceMenu.create` and `workspaceRefusedAuthority` are
      still read by `workspace-dialog.svelte`, `workspaces.svelte` and `area.svelte`.*
- [x] `layout/tests/workspace-menu.svelte.test.ts` finds the header, the radio rows with the
      open one checked, one workspaces row, and no element carrying
      `data-workspace-menu-invite`, `data-workspace-menu-create` or
      `data-workspace-menu-invite-refusal`; the sidebar's test, where it asserted the refusal
      sentences, no longer does. *Verified: `pnpm exec vitest run
      src/lib/layout/tests/workspace-menu.svelte.test.ts` printed `Test Files 1 passed (1)`,
      `Tests 7 passed (7)`, including `one row leads to the settings area at the workspaces
      section` and `nothing in the menu invites anybody or makes a workspace`.*
- [x] The invite dialog and the workspace dialog still open from the members and workspaces
      sections through `openOrganizationDialog`; `layout/component/organization-dialogs.svelte`
      is unchanged. *Verified: `git diff HEAD~1 HEAD --stat` on `organization-dialogs.svelte`
      prints nothing; `openOrganizationDialog('invite')` is called from `members.svelte` and
      `openOrganizationDialog('workspace')` from `workspaces.svelte`.*
- [x] `pnpm check`, `pnpm lint` and `pnpm test` pass; a changeset for `@rentable/desktop`
      rides with the change. *Verified in the run's worktree: `pnpm check` exit 0 (desktop
      `9302 FILES 0 ERRORS 0 WARNINGS`, design `2807 FILES 0 ERRORS`), `pnpm lint` exit 0,
      `pnpm test` exit 0 (`Tasks: 4 successful`, desktop `176 passed`, design `58 passed`);
      `.changeset/the-workspace-menu-is-the-workspace-and-the-switch.md` is in the commit.*

## Relevant areas

`apps/desktop/src/lib/layout/component/{workspace-menu,sidebar}.svelte`,
`apps/desktop/src/lib/layout/tests/workspace-menu.svelte.test.ts`,
`apps/desktop/src/lib/i18n/{en,ar}/index.ts`, `apps/desktop/src/lib/i18n/i18n-types.ts`.

## Constraints

- **Read [[efforts/828-the-link-needs-a-code-and-the-settings-area-guides/plan]], *The two
  menus*.**
- The menu's docstring is rewritten to say what the menu is now and to date what it was;
  the ClickUp reference of 2026-08-20 and 826's requirement 17 are named as what it was.
- Strings that retire are removed, never left as unused keys.

## Notes
