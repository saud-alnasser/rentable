//! scaffolding the Google tests share.
//!
//! A directory rather than a file, because what tests need in common grows and the
//! alternative is a filename that names two things.

pub(in crate::sync) mod server;
