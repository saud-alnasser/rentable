/tauri/target/

# Generated by `tauri/build.rs` from `packages/workspace-migrations`, which is the one tracked
# copy. The Rust runner takes a directory and Tauri bundles one, so the files have to be here —
# but they are mirrored on every cargo build rather than committed twice.
/tauri/migrations/
