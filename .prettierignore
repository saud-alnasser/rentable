# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/apps/desktop/static/

build
**/tauri/target

# i18n
**/src/lib/i18n/i18n-*.ts

# Turborepo's local cache
.turbo

# Agent configuration. Most files under here are installed verbatim from the
# workflow's distribution, so reflowing any of them to printWidth would break
# that byte-for-byte comparison and make every update show a spurious diff.
# `CLAUDE.md` and `AGENTS.md` are deliberately not listed: they are authored
# here and stay format-enforced with the rest of the repository's root.
/.aep/
/.claude/
